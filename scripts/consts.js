import path from "path";

export const ROOT = process.cwd();
export const PUBLIC_DIR = path.join(ROOT, "public");
export const DATA_SRC = path.join(ROOT, "data");
export const IMAGES_SRC = path.join(ROOT, "images");
